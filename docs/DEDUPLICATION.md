# Deduplication

PieAtlas groups recipes into families but preserves every historical variant.

Use:

- `family_id`: common recipe family, such as `apple-pie`
- `duplicate_status`: `unique_variant`, `near_duplicate`, `exact_reprint`, or `needs_review`

Do not delete historical variants. Keep each source recipe row.
