# Commit to PieAtlas

From a terminal:

```bash
git clone https://github.com/micklelow-source/PieAtlas.git
cd PieAtlas
unzip /path/to/PieAtlas-rev1-starter.zip
git add .
git commit -m "Initialize PieAtlas Rev1 roadmap and harvest framework"
git push origin main
```

Because the repo is empty, this package is designed as the first commit.
